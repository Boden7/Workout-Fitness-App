<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <LinearLayout
        android:orientation="vertical"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Back Button -->
        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_back"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Back" />

        <!-- Title -->
        <TextView
            android:id="@+id/tvOtherActivitiesTitle"
            android:text="Other Activities Videos"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingTop="16dp"
            android:paddingBottom="16dp" />

        <!-- ===== VIDEO CARD 10 ===== -->
        <LinearLayout
            android:id="@+id/cardVideo10"
            android:orientation="vertical"
            android:padding="12dp"
            android:background="@drawable/rounded_bg"
            android:layout_marginBottom="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/video10"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:adjustViewBounds="true"
                android:src="@drawable/video10"
                android:scaleType="fitCenter" />
        </LinearLayout>

        <!-- ===== VIDEO CARD 11 ===== -->
        <LinearLayout
            android:id="@+id/cardVideo11"
            android:orientation="vertical"
            android:padding="12dp"
            android:background="@drawable/rounded_bg"
            android:layout_marginBottom="20dp"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="true">

            <ImageView
                android:id="@+id/video11"
                android:layout_width="match_parent"
                android:layout_height="185dp"
                android:adjustViewBounds="true"
                android:scaleType="fitCenter"
                android:src="@drawable/video11" />
        </LinearLayout>

    </LinearLayout>

</ScrollView>
